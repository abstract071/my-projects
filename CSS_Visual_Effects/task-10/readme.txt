Box Shadow

The CSS3 backgrounds and borders module has a nice new feature called box-shadow, which is implemented in Safari 3+, Chrome and Firefox 3.
Shadows can be applied in CSS3 to borders, images as well as affecting text as well.
